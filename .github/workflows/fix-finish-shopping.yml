name: Legacy prototype rebuild (manual only)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy rebuild disabled. index.html is now the prototype source of truth."
